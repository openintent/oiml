version: 0.1.0
ai_context:
  purpose: Conform to project frameworks, style, and conventions.
  instructions: |
    - Read .openintent/AGENTS.md and apply the instructions to the intent.
    - Update the existing GET /api/v1/artists endpoint to include associated albums in the response.
    - Use Ent's WithAlbums() method to eager load the albums relation.
  references:
    - kind: file
      path: .openintent/AGENTS.md
provenance:
  created_by:
    type: human
    name: ""
  created_at: 2025-01-27T00:00:00Z

intents:
  - kind: update_endpoint
    scope: api
    method: GET
    path: /api/v1/artists
    updates:
      add_field:
        - name: albums
          source:
            type: relation
            relation: albums
    description: Update GET /api/v1/artists to include associated albums in the response
